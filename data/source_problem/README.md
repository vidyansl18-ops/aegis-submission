# KMIT Source Test Inputs

These JSON/text files reproduce the structured inputs shown in the supplied KMIT problem statement. The only additional runtime metadata is the simulator policy that enables the problem statement's advertised stop-idle action for a worker with zero traffic.
